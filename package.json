{
  "name": "webcam-robot",
  "private": true,
  "workspaces": [
    "packages/*",
    "app",
    "server"
  ],
  "scripts": {
    "upload": "rsync -avz --exclude node_modules --exclude .git --exclude dist --exclude out . ubuntu@192.168.0.140:~/webcam-robot",
    "download": "rsync -avz --exclude node_modules --exclude .git --exclude dist --exclude out ubuntu@192.168.0.140:~/webcam-robot/ ."
  }
}
